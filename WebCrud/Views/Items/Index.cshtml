@model List<WebCrud.Models.Item>

<h2>RP 1 Items List</h2>
<h2>RP 2 Items List</h2>
<h2>RP 3 Items List schedule</h2>
<h2>RP 4 Items List schedule check </h2>

<p><a asp-action="Create">Create New Item</a></p>

<table border="1">
    <tr>
        <th>ID</th>
        <th>Name</th>
        <th>Description</th>
        <th>Actions</th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Id</td>
            <td>@item.Name</td>
            <td>@item.Description</td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
    }
</table>
